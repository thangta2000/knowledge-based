export type Height = {
  l: number;
  m: number;
  h: number;
};

export type Weight = {
  l: number;
  m: number;
  h: number;
};

export type Force = {
  y: number;
  d: number;
  t: number;
};

export type Jump = {
  y: number;
  d: number;
  t: number;
};

export enum Gender {
  male = "male",
  female = "female",
}
